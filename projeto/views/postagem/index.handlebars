<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <small>Data publicação: {{postagem.data}}</small>
        <hr>
        <p>{{postagem.conteudo}}</p>
        <hr>
        <small>Categoria: {{ postagem.slug}}</small>
    </div>
</div>